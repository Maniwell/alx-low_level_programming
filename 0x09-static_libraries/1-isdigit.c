#include "main.h"

/**
  * _isdigit - checks if a number is found 0 through 9
  * @c: is the character to be checked
  *
  * Return: 0 if not found or 1 if found
  */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')

	return (1);

	else

	return (0);
}
