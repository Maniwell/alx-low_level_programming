#include "main.h"
#include <stdlib.h>
/**
  * _abs - gives the absolute value of input integers
  *@num: input integer
  *
  *Return: success if 0
  **/

int _abs(int num)
{
	int num_abs = abs(num);

	return (num_abs);
}

