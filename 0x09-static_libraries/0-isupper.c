#include "main.h"

/**
  * _isupper - uppercase letters
  * @c: is a character to be checked
  *
  * Return: 0 if not uppercase or 1 if upper case
  */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

	return (1);

	else

	return (0);
}
